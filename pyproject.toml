[project]
name = "idachat"
version = "0.1.0"
description = "CLI chat interface for IDA Pro using Claude Agent SDK"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk",
    "ida-domain",
]

[project.scripts]
idachat = "ida_chat_cli:main"
